// $('#menu_id').on('change', function(){
//     @foreach ($menu as $item)
//         if($(this).value() === $item->menu_id){
//             $("#menu_price").show()
//         }else{
//             $("#menu_price").hide()
//         }
//     @endforeach
// })
const menu = document.getElementById("menu_id")
const field = document.getElementById("menu_price")

menu.addEventListener("change", function(){
    
    if(menu.value == ){

    }
})

